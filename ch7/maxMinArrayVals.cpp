#include <iostream>
#include <fstream>
#include <string>
using namespace std;

int getMax(int arr[]) {
    for (int i = 0; arr.length(); i++) {
        
    }
}

int main() {
    int SIZE = 10;
    int input;

    int array[SIZE];
    
    for(int i = 0; i < SIZE; i++) {
        cin >> input;
        array[i] = input;

    }

    int max = getMax(array);



}